// The only line that changes between environments. For local development point
// this at your running Flask server. For production paste your Render URL here.
// No trailing slash.
export const API_BASE = "http://localhost:5000";
